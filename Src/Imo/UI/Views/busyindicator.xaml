<UserControl x:Class="ImoSilverlightApp.UI.Views.BusyIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
             xmlns:converters="clr-namespace:ImoSilverlightApp.Converters"
             mc:Ignorable="d"
             x:Name="busyIndicatorRoot"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             d:DesignHeight="480" d:DesignWidth="480">
    <Grid>
        <ProgressBar IsIndeterminate="True" VerticalAlignment="Center" Height="50" Width="250" Background="Transparent" />
    </Grid>
</UserControl>
